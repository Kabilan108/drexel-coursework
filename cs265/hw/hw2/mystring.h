// Author: Tony K. Okeke
// Date:   03.13.2024

#ifndef MYSTRING_H
#define MYSTRING_H

int mystrlen(char* str);

int mystrcmp(char* str1, char* str2);
// strcmp compares two C strings character by character. If the strings are 
// identical, it returns 0. If the first string is lexicographically less than 
// the second, it returns a negative value, and if it's greater, it returns a 
// positive value.

#endif
